# Archived self-improvement implementation plans

This directory keeps completed, absorbed, and superseded implementation plans for audit and regression context. Do not continue an archived plan directly; create a new timestamped plan in `.hermes/plans/` if a slice needs to be reopened.

Root `.hermes/plans/README.md` is the source of truth for current active/completed status and retained decisions.
